#EventEaseApp created using Microsoft Co-pilot

Simple Event Ease app that helps to track events list, register a user to an event, track attendance.

It uses local storage of a browser to persist the data.
